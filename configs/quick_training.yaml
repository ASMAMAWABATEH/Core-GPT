training:
  batch_size: 32
  learning_rate: 0.0003
  max_training_steps: 200
  eval_interval: 50
  save_interval: 50
  grad_clip: 1.0
  seed: 1337
  device: auto
  weight_decay: 0.01
  num_workers: 0
  log_interval: 10
  warmup_steps: 20
  checkpoint_dir: checkpoints
