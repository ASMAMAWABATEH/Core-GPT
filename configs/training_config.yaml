training:
  batch_size: 32
  learning_rate: 0.0003
  max_training_steps: 5000
  eval_interval: 500
  save_interval: 1000
  grad_clip: 1.0
  seed: 1337
  device: auto
  weight_decay: 0.01
  num_workers: 0
  log_interval: 50
  warmup_steps: 100
  checkpoint_dir: checkpoints
